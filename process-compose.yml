version: "0.5"

processes:
  adminer:
   command: "frankenphp php-server --listen ${HOST}:18080"
   availability:
    restart: "always"
   readiness_probe:
    http_get:
     host: ${HOST}
     port: 18080
